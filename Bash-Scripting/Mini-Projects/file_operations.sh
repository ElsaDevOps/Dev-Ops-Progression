#!/bin/bash

mkdir operation
cd operation
touch test.txt
echo "This is a test" > test.txt
cat test.txt

# This script creates a directory called 'operation'. It then navigates to that directory,creates a file in that directory, weites text and directs it into the file, then displays the contents of the file.
